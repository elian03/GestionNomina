/**
 * Represents a batch
 *
 * @export
 * @class Batch
 */
export class Product {
    id: number;
    productName: string;
    description: string;
    quantityStoraged: number;
    purchasePrice: number;
    salePrice: number;
}
